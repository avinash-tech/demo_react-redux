import siteSetting from './index';

const url = siteSetting.api.url;

export default {
  

        'EMPLOYEE_DUMMY':`${url}employees`,
        "ADD_EMPLOYEE": `${url}create`,
        'EDIT_EMPLOYEE':`${url}update/21`

   
}
